# blank

An empty Python project. Ask the agent to build something from scratch: it has pytest available,
so a good task names the module to create and the behaviour its tests should cover.

Run the tests with `python -m pytest -q`.
